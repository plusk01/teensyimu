from setuptools import setup

setup(
    name='acl_serial_driver',
    version='0.1',
    author='Parker Lusk',
    author_email='plusk@mit.edu',
    description='ACL serial driver for Teensy-IMU',
    package_dir={'': '${CMAKE_CURRENT_BINARY_DIR}'},
    packages=['acl_serial_driver'],
    package_data={'': ['*.so']}
)